{
	"name": "phone-line",
	"version": "1.0.0",
	"scripts": {
		"frontend": "npm run dev --prefix frontend",
		"backend": "node cardknox-backend/server.js",
		"dev": "concurrently \"npm run frontend\" \"npm run backend\""
	},
	"devDependencies": {
		"concurrently": "^8.2.2"
	}
}
